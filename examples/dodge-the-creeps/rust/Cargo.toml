[package]
name = "dodge-the-creeps"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[features]
default = ["minimal"]
minimal = ["godot/minimal"]

[dependencies]
godot = { path = "../../../godot" }
rand = "0.8"
